
/**
 * @format
 */
import { AppRegistry } from 'react-native';
import App from './App';

// The name here should match the name of your project in app.json
const appName = 'CardWalletApp'; 

AppRegistry.registerComponent(appName, () => App);
